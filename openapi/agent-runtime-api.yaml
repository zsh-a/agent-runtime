openapi: 3.1.0
info:
  title: Agent Runtime API
  version: 0.1.0
  description: Minimal HTTP surface for the schema-first Rust agent runtime.
jsonSchemaDialect: https://json-schema.org/draft/2020-12/schema
paths:
  /healthz:
    get:
      operationId: healthz
      responses:
        "200":
          description: Runtime process is alive.
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status:
                    const: ok
                additionalProperties: false
  /catalog/summary:
    get:
      operationId: catalogSummary
      responses:
        "200":
          description: Summary of the active agent catalog.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CatalogSummary"
  /metrics/summary:
    get:
      operationId: metricsSummary
      responses:
        "200":
          description: Store-backed runtime metrics summary.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RuntimeMetricsSummary"
        "500":
          description: Metrics collection failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /chat/turn:
    post:
      operationId: streamChatTurn
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "../schemas/chat-turn-request.schema.json"
      responses:
        "200":
          description: Server-sent event stream of chat turn events.
          content:
            text/event-stream:
              schema:
                type: string
                description: Each SSE data frame is a ChatTurnEvent JSON object from chat-turn-event.schema.json.
        "400":
          description: Request body failed JSON or schema validation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Chat turn failed before streaming started.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /chat/resume:
    post:
      operationId: streamChatResume
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "../schemas/chat-resume-request.schema.json"
      responses:
        "200":
          description: Server-sent event stream of resumed chat turn events.
          content:
            text/event-stream:
              schema:
                type: string
                description: Each SSE data frame is a ChatTurnEvent JSON object from chat-turn-event.schema.json.
        "400":
          description: Request body failed JSON or schema validation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Chat resume failed before streaming started.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /workflows/run:
    post:
      operationId: runWorkflow
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "../schemas/workflow-run-request.schema.json"
      responses:
        "200":
          description: Completed workflow DAG invocation result.
          content:
            application/json:
              schema:
                $ref: "../schemas/workflow-run-result.schema.json"
        "400":
          description: Request body failed JSON or schema validation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Workflow invocation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /tools:
    get:
      operationId: listTools
      responses:
        "200":
          description: Active tools visible to the runtime.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "../schemas/tool-spec.schema.json"
  /runs:
    get:
      operationId: listRuns
      parameters:
        - name: agent_id
          in: query
          required: false
          schema:
            type: string
            minLength: 1
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
      responses:
        "200":
          description: Persisted run records, newest first.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AgentRunRecord"
        "500":
          description: Run listing failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /runs/{run_id}:
    get:
      operationId: inspectRun
      parameters:
        - name: run_id
          in: path
          required: true
          schema:
            type: string
            pattern: "^run_"
      responses:
        "200":
          description: Persisted run record.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentRunRecord"
        "404":
          description: Run was not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /runs/{run_id}/trace:
    get:
      operationId: inspectRunTrace
      parameters:
        - name: run_id
          in: path
          required: true
          schema:
            type: string
            pattern: "^run_"
      responses:
        "200":
          description: Persisted run trace.
          content:
            application/json:
              schema:
                $ref: "../schemas/trace.schema.json"
        "404":
          description: Trace was not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /runs/{run_id}/events:
    get:
      operationId: streamRunTraceEvents
      parameters:
        - name: run_id
          in: path
          required: true
          schema:
            type: string
            pattern: "^run_"
        - name: after
          in: query
          required: false
          description: Resume after this run event cursor. Query parameter takes precedence over Last-Event-ID.
          schema:
            type: integer
            minimum: 0
        - name: follow
          in: query
          required: false
          description: When false, return the currently available event snapshot and close the stream.
          schema:
            type: boolean
            default: true
        - name: Last-Event-ID
          in: header
          required: false
          description: Standard SSE resume cursor used when the after query parameter is absent.
          schema:
            type: string
      responses:
        "200":
          description: Server-sent event stream of active run events when the run is live, otherwise persisted trace events.
          content:
            text/event-stream:
              schema:
                type: string
                description: Each SSE data frame is a TraceEvent JSON object from trace.schema.json. SSE id fields are monotonically increasing run event cursors.
        "404":
          description: Trace was not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /runs/{run_id}/cancel:
    post:
      operationId: cancelRun
      parameters:
        - name: run_id
          in: path
          required: true
          schema:
            type: string
            pattern: "^run_"
      responses:
        "200":
          description: Cancellation request result. Active runs receive cancellation and running records persist a cancellation intent under metadata.control.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CancelRunResponse"
        "404":
          description: Run was not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /runs/{run_id}/replay:
    post:
      operationId: replayRun
      parameters:
        - name: run_id
          in: path
          required: true
          schema:
            type: string
            pattern: "^run_"
      responses:
        "200":
          description: Live replay result generated from the persisted trace.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplayExecutionResponse"
        "500":
          description: Replay failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /tools/{tool_name}/call:
    post:
      operationId: callTool
      parameters:
        - name: tool_name
          in: path
          required: true
          schema:
            type: string
            minLength: 1
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                input:
                  type: object
                  default: {}
              additionalProperties: false
      responses:
        "200":
          description: Tool call output envelope.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ToolCallResponse"
        "500":
          description: Tool call failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /proposals:
    get:
      operationId: listProposals
      parameters:
        - name: run_id
          in: query
          required: false
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Persisted proposal envelopes.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "../schemas/proposal-envelope.schema.json"
    post:
      operationId: createProposal
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProposalCreateRequest"
      responses:
        "200":
          description: Created proposal envelope.
          content:
            application/json:
              schema:
                $ref: "../schemas/proposal-envelope.schema.json"
        "403":
          description: Proposal creation was denied by policy.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Proposal creation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /proposals/{proposal_id}:
    get:
      operationId: inspectProposal
      parameters:
        - name: proposal_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Proposal envelope.
          content:
            application/json:
              schema:
                $ref: "../schemas/proposal-envelope.schema.json"
        "404":
          description: Proposal was not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /proposals/{proposal_id}/decision:
    post:
      operationId: decideProposal
      parameters:
        - name: proposal_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProposalDecisionRequest"
      responses:
        "200":
          description: Approval decision and updated proposal envelope.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProposalDecisionResponse"
        "500":
          description: Proposal decision failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /proposals/{proposal_id}/apply:
    post:
      operationId: applyProposal
      parameters:
        - name: proposal_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Proposal apply result and updated proposal envelope.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProposalActionResponse"
        "403":
          description: Proposal apply was denied by policy.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Proposal apply failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /proposals/{proposal_id}/undo:
    post:
      operationId: undoProposal
      parameters:
        - name: proposal_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Proposal undo result and updated proposal envelope.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProposalActionResponse"
        "500":
          description: Proposal undo failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /sessions:
    get:
      operationId: listSessions
      responses:
        "200":
          description: Persisted debugging sessions.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "../schemas/session-record.schema.json"
    post:
      operationId: createSession
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SessionCreateRequest"
      responses:
        "200":
          description: Created session and root thread.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionCreateResponse"
        "500":
          description: Session creation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /sessions/{session_id}:
    get:
      operationId: showSession
      parameters:
        - name: session_id
          in: path
          required: true
          schema:
            type: string
            pattern: "^session_"
      responses:
        "200":
          description: Session, threads, and recorded steps.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionShowResponse"
        "404":
          description: Session was not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /sessions/{session_id}/fork:
    post:
      operationId: forkSessionThread
      parameters:
        - name: session_id
          in: path
          required: true
          schema:
            type: string
            pattern: "^session_"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ThreadForkRequest"
      responses:
        "200":
          description: Created child thread.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ThreadForkResponse"
        "500":
          description: Thread fork failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /agents/{agent_id}/run:
    post:
      operationId: runAgent
      parameters:
        - name: agent_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "../schemas/http-agent-run-request.schema.json"
      responses:
        "200":
          description: Completed runtime invocation envelope.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentRunResponse"
        "400":
          description: Request body failed JSON or schema validation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Runtime invocation failed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
components:
  schemas:
    CatalogSummary:
      type: object
      required:
        - protocol_version
        - catalog_version
        - generated_at
        - active_domains
        - agent_count
        - tool_count
        - proposal_kind_count
        - prompt_block_count
      properties:
        protocol_version:
          const: agent.v1
        catalog_version:
          const: agent_catalog.v1
        generated_at:
          type: string
          format: date-time
        active_domains:
          type: array
          items:
            type: string
        agent_count:
          type: integer
          minimum: 0
        tool_count:
          type: integer
          minimum: 0
        proposal_kind_count:
          type: integer
          minimum: 0
        prompt_block_count:
          type: integer
          minimum: 0
      additionalProperties: false
    AgentRunResponse:
      type: object
      required: [result, trace]
      properties:
        result:
          $ref: "../schemas/run-result.schema.json"
        trace:
          $ref: "../schemas/trace.schema.json"
      additionalProperties: false
    CancelRunResponse:
      type: object
      required: [cancellation_requested, message, run_id]
      properties:
        cancellation_requested:
          type: boolean
          description: True when an active run was signaled or a persisted running record was marked for cancellation.
        message:
          type: string
        run_id:
          type: string
          pattern: "^run_"
        status:
          type: string
          enum:
            - running
            - completed
            - skipped
            - failed
            - cancelled
            - timed_out
            - abandoned
      additionalProperties: false
    AgentRunRecord:
      type: object
      required:
        - protocol_version
        - run_id
        - agent_id
        - status
        - scope
        - started_at
        - input
        - output
      properties:
        protocol_version:
          const: agent.v1
        run_id:
          type: string
          pattern: "^run_"
        idempotency_key:
          type: string
          pattern: "^idem_[a-f0-9]{64}$"
          description: Stable retry identity derived from agent, scope, trigger, scheduled_for, and trigger envelope source/id when present.
        agent_id:
          type: string
          minLength: 1
        status:
          type: string
          enum:
            - running
            - completed
            - skipped
            - failed
            - cancelled
            - timed_out
            - abandoned
        scope:
          type: object
          required: [type]
          properties:
            type:
              type: string
              enum: [global, user, tenant]
            id:
              type: string
          additionalProperties: false
        started_at:
          type: string
          format: date-time
        finished_at:
          type: ["string", "null"]
          format: date-time
        input:
          type: object
        output:
          type: object
        error:
          type: ["object", "null"]
        workflow:
          $ref: "#/components/schemas/RunWorkflow"
        metadata:
          type: object
          default: {}
      additionalProperties: false
    RunWorkflow:
      type: object
      properties:
        workflow_id:
          type: ["string", "null"]
          minLength: 1
        root_run_id:
          type: ["string", "null"]
          minLength: 1
        parent_run_id:
          type: ["string", "null"]
          minLength: 1
        parent_agent_id:
          type: ["string", "null"]
          minLength: 1
        dependencies:
          type: array
          items:
            $ref: "#/components/schemas/RunDependency"
          default: []
        fanout_id:
          type: ["string", "null"]
          minLength: 1
        fanin_id:
          type: ["string", "null"]
          minLength: 1
        compensation:
          $ref: "#/components/schemas/RunCompensation"
        metadata:
          type: object
          default: {}
      additionalProperties: false
    RunDependency:
      type: object
      required: [run_id]
      properties:
        run_id:
          type: string
          minLength: 1
        edge:
          type: ["string", "null"]
          minLength: 1
        metadata:
          type: object
          default: {}
      additionalProperties: false
    RunCompensation:
      type: object
      required: [compensates_run_id]
      properties:
        compensates_run_id:
          type: string
          minLength: 1
        strategy:
          type: ["string", "null"]
          minLength: 1
        metadata:
          type: object
          default: {}
      additionalProperties: false
    ReplayExecutionResponse:
      type: object
      required:
        - mode
        - source_run_id
        - replay_run_id
        - agent_id
        - result
        - trace
        - output_matches
      properties:
        mode:
          type: string
          enum: [view, deterministic, live]
        source_run_id:
          type: string
          pattern: "^run_"
        replay_run_id:
          type: string
          pattern: "^run_"
        agent_id:
          type: string
          minLength: 1
        result:
          $ref: "../schemas/run-result.schema.json"
        trace:
          $ref: "../schemas/trace.schema.json"
        output_matches:
          type: boolean
      additionalProperties: false
    ToolCallResponse:
      type: object
      required: [tool, output]
      properties:
        tool:
          type: string
          minLength: 1
        output:
          type: object
      additionalProperties: false
    RuntimeMetricsSummary:
      type: object
      required:
        - protocol_version
        - runtime_version
        - generated_at
        - store_root
        - run_count
        - runs_by_status
        - successful_run_count
        - skipped_run_count
        - failed_run_count
        - timeout_count
        - total_run_latency_ms
        - tool_call_count
        - failed_tool_call_count
        - total_tool_call_latency_ms
        - replay_count
        - proposal_count
        - proposals_by_status
        - proposal_created_count
        - proposal_approved_count
        - proposal_denied_count
        - proposal_applied_count
        - artifact_ref_count
        - llm_total_tokens
        - runs_by_agent
        - tool_calls_by_tool
        - llm_usage_by_provider
      properties:
        protocol_version:
          const: agent.v1
        runtime_version:
          type: string
          minLength: 1
        generated_at:
          type: string
          format: date-time
        store_root:
          type: string
          minLength: 1
        run_count:
          type: integer
          minimum: 0
        runs_by_status:
          type: object
          additionalProperties:
            type: integer
            minimum: 0
        successful_run_count:
          type: integer
          minimum: 0
        skipped_run_count:
          type: integer
          minimum: 0
        failed_run_count:
          type: integer
          minimum: 0
        timeout_count:
          type: integer
          minimum: 0
        total_run_latency_ms:
          type: integer
          minimum: 0
        average_run_latency_ms:
          type: ["number", "null"]
        tool_call_count:
          type: integer
          minimum: 0
        failed_tool_call_count:
          type: integer
          minimum: 0
        total_tool_call_latency_ms:
          type: integer
          minimum: 0
        average_tool_call_latency_ms:
          type: ["number", "null"]
        replay_count:
          type: integer
          minimum: 0
        proposal_count:
          type: integer
          minimum: 0
        proposals_by_status:
          type: object
          additionalProperties:
            type: integer
            minimum: 0
        proposal_created_count:
          type: integer
          minimum: 0
        proposal_approved_count:
          type: integer
          minimum: 0
        proposal_denied_count:
          type: integer
          minimum: 0
        proposal_applied_count:
          type: integer
          minimum: 0
        artifact_ref_count:
          type: integer
          minimum: 0
        llm_total_tokens:
          type: integer
          minimum: 0
        runs_by_agent:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/RuntimeAgentMetrics"
        tool_calls_by_tool:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/RuntimeToolMetrics"
        llm_usage_by_provider:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/RuntimeLlmProviderMetrics"
      additionalProperties: false
    RuntimeAgentMetrics:
      type: object
      required:
        - run_count
        - runs_by_status
        - successful_run_count
        - failed_run_count
        - total_run_latency_ms
      properties:
        run_count:
          type: integer
          minimum: 0
        runs_by_status:
          type: object
          additionalProperties:
            type: integer
            minimum: 0
        successful_run_count:
          type: integer
          minimum: 0
        failed_run_count:
          type: integer
          minimum: 0
        total_run_latency_ms:
          type: integer
          minimum: 0
        average_run_latency_ms:
          type: ["number", "null"]
      additionalProperties: false
    RuntimeToolMetrics:
      type: object
      required:
        - tool_call_count
        - failed_tool_call_count
        - total_tool_call_latency_ms
      properties:
        tool_call_count:
          type: integer
          minimum: 0
        failed_tool_call_count:
          type: integer
          minimum: 0
        total_tool_call_latency_ms:
          type: integer
          minimum: 0
        average_tool_call_latency_ms:
          type: ["number", "null"]
      additionalProperties: false
    RuntimeLlmProviderMetrics:
      type: object
      required:
        - request_count
        - input_tokens
        - output_tokens
        - total_tokens
        - total_latency_ms
        - cost_micros_by_currency
      properties:
        request_count:
          type: integer
          minimum: 0
        input_tokens:
          type: integer
          minimum: 0
        output_tokens:
          type: integer
          minimum: 0
        total_tokens:
          type: integer
          minimum: 0
        total_latency_ms:
          type: integer
          minimum: 0
        average_latency_ms:
          type: ["number", "null"]
        cost_micros_by_currency:
          type: object
          additionalProperties:
            type: integer
            minimum: 0
      additionalProperties: false
    HookEvent:
      $ref: "../schemas/hook-event.schema.json"
    PromptManifest:
      $ref: "../schemas/prompt-manifest.schema.json"
    ProposalCreateRequest:
      type: object
      required: [run_id, agent_id, kind, summary]
      properties:
        run_id:
          type: string
          minLength: 1
        agent_id:
          type: string
          minLength: 1
        kind:
          type: string
          minLength: 1
        summary:
          type: string
          minLength: 1
        payload:
          type: object
          default: {}
        diffs:
          type: array
          default: []
          items:
            $ref: "#/components/schemas/ProposalDiff"
        warnings:
          type: array
          default: []
          items:
            $ref: "#/components/schemas/ProposalWarning"
      additionalProperties: false
    ProposalDiff:
      type: object
      required: [path]
      properties:
        path:
          type: string
          minLength: 1
        operation:
          type: string
          enum: [add, remove, replace]
          default: replace
        before: {}
        after: {}
        metadata:
          type: object
          default: {}
      additionalProperties: false
    ProposalWarning:
      type: object
      required: [code, message]
      properties:
        severity:
          type: string
          enum: [info, warning, danger]
          default: warning
        code:
          type: string
          minLength: 1
        message:
          type: string
          minLength: 1
        metadata:
          type: object
          default: {}
      additionalProperties: false
    ProposalDecisionRequest:
      type: object
      required: [decision]
      properties:
        decision:
          type: string
          enum: [approve, approved, deny, denied]
        approval_level:
          type: string
          enum: [none, single_user, multi_approver, admin]
        decided_by:
          type: [string, "null"]
          minLength: 1
        comment:
          type: [string, "null"]
      additionalProperties: false
    ProposalDecisionResponse:
      type: object
      required: [decision, proposal]
      properties:
        decision:
          $ref: "../schemas/approval-decision.schema.json"
        proposal:
          $ref: "../schemas/proposal-envelope.schema.json"
      additionalProperties: false
    ProposalActionResponse:
      type: object
      required: [action, tool, tool_output, proposal]
      properties:
        action:
          type: string
          enum: [apply, undo]
        tool:
          type: string
          minLength: 1
        tool_output:
          type: object
        proposal:
          $ref: "../schemas/proposal-envelope.schema.json"
      additionalProperties: false
    SessionCreateRequest:
      type: object
      required: [title]
      properties:
        title:
          type: string
          minLength: 1
        metadata:
          type: object
          default: {}
      additionalProperties: false
    SessionCreateResponse:
      type: object
      required: [session, thread]
      properties:
        session:
          $ref: "../schemas/session-record.schema.json"
        thread:
          $ref: "../schemas/thread-record.schema.json"
      additionalProperties: false
    ThreadWithSteps:
      type: object
      required: [thread, steps]
      properties:
        thread:
          $ref: "../schemas/thread-record.schema.json"
        steps:
          type: array
          items:
            $ref: "../schemas/step-record.schema.json"
      additionalProperties: false
    SessionShowResponse:
      type: object
      required: [session, threads]
      properties:
        session:
          $ref: "../schemas/session-record.schema.json"
        threads:
          type: array
          items:
            $ref: "#/components/schemas/ThreadWithSteps"
      additionalProperties: false
    ThreadForkRequest:
      type: object
      required: [parent_thread_id]
      properties:
        parent_thread_id:
          type: string
          pattern: "^thread_"
        title:
          type: string
        metadata:
          type: object
          default: {}
      additionalProperties: false
    ThreadForkResponse:
      type: object
      required: [session_id, parent_thread_id, thread]
      properties:
        session_id:
          type: string
          pattern: "^session_"
        parent_thread_id:
          type: string
          pattern: "^thread_"
        thread:
          $ref: "../schemas/thread-record.schema.json"
      additionalProperties: false
    ErrorResponse:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
          minLength: 1
        message:
          type: string
          minLength: 1
        details:
          type: object
          description: Optional structured error details. Policy denials include the hook event and proposal context.
      additionalProperties: false
